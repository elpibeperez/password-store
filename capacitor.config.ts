import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.passmanager.app',
  appName: 'password-store',
  webDir: 'capacitor/www/dist'
};

export default config;
